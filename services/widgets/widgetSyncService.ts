/**
 * Widget Sync Service
 *
 * Widgets are temporarily disabled.
 * All exported functions are intentionally no-ops.
 */

/**
 * Widgets disabled — do nothing.
 */
export async function syncWidgetStreak(): Promise<void> {
  return;
}

/**
 * Widgets disabled — do nothing.
 */
export function syncWidgetReflectionPrompt(
  _prompt: string,
  _category: string,
): void {
  return;
}
